import 'package:flutter/material.dart';

const backgroundColor = Color(0xFF0E0E0E); // 배경색
const primaryColor = Colors.white; // 메인 컬러
final secondaryColor = Colors.grey[600]; // 서브 컬러 - [600]이라는 키값을 넣어서 런타임에 색상이 계산되기 때문에 const 사용 불가

